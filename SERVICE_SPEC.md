# MyTasks - サービス仕様書

## 1. サービス概要
- **一言で**: HTMLファイル1枚で動く、自分専用のタスク管理アプリ。ログイン不要・オフライン動作。
- **対象ユーザー**: 複数のプロジェクトを並行で抱える個人（開発者・フリーランス・小規模事業者）。
- **解決する課題**: プロジェクトをまたいだ「自分のタスク」が、Slack・メモ・頭の中に散らばって、今日やることが確定しない。チーム用のタスク管理ツールを立ち上げるほどでもない粒度のタスクを、1画面で完結させる。
- **ステータス**: MVP（動作確認済み）

## 2. ユーザーロールと権限
| ロール | できること |
|---|---|
| 本人（唯一のロール） | 全機能。データはブラウザの localStorage にのみ保存され、外部送信は一切ない |

## 3. 機能一覧

### 実装済み
- [x] クイック追加 — 1行の自然入力からプロジェクト・期限・優先度・繰り返しを解釈
- [x] 4ビュー — 今日 / 今週 / すべて / 完了
- [x] 期限バケット表示 — 期限切れ・今日・明日・今週・これから・期限なしで自動グルーピング
- [x] プロジェクトフィルタ — 未完了件数つきチップで絞り込み
- [x] インライン編集 — 行クリックでタイトル/プロジェクト/期限/優先度/繰り返し/メモを編集
- [x] 期限クイック操作 — 今日 / 明日 / +1週 / 期限なし
- [x] 繰り返しタスク — 完了時に次回分（毎日/毎週/毎月）を自動生成
- [x] 削除の取り消し — トーストから7秒以内に復元
- [x] 統計 — 未完了 / 今日やる / 期限切れ / 今週完了
- [x] ダーク・ライトテーマ（OS設定に追従＋手動切替、設定は保存）
- [x] JSON 書き出し / 読み込み（ID重複はスキップしてマージ）
- [x] Markdown コピー（`- [ ] タスク` 形式。朝礼・日報に貼れる）
- [x] 古い完了の整理（完了から30日超を削除）
- [x] キーボードショートカット `n`（追加欄）/ `Esc`（編集を閉じる）
- [x] レスポンシブ（スマホ1カラム）
- [x] 演出 — オーロラ背景・ガラスUI・完了チェック描画＋パーティクル・今日全完了で紙吹雪・入力中の解釈チッププレビュー・タブのスライドピル・テーマ切替の円形リビール・今日の完了率リング（すべて reduced-motion で無効化）

### 未実装（予定）
- [ ] 複数端末同期（現状は端末×ブラウザ単位。同期は JSON 書き出し/読み込みで代替）
- [ ] サブタスク・チェックリスト
- [ ] 実績時間の記録
- [ ] 期限の時刻指定・リマインド通知

## 4. 画面一覧
| 画面 | パス | 対象ロール | 概要 |
|---|---|---|---|
| メイン | `index.html` | 本人 | 統計・追加欄・ビュー切替・タスク一覧・ツールを1画面に集約（SPA、画面遷移なし） |

## 5. データモデル

localStorage キー `mytasks.v1` に、タスクの配列を JSON で保存する。

- `task`
  - `id` — 文字列。生成時のタイムスタンプ+乱数
  - `title` — 文字列。タスク名
  - `note` — 文字列。メモ（複数行可）
  - `project` — 文字列。プロジェクト名（空可）
  - `due` — 文字列。`YYYY-MM-DD`（空 = 期限なし）
  - `priority` — `urgent` | `high` | `normal` | `low`
  - `repeat` — `none` | `daily` | `weekly` | `monthly`
  - `done` — 真偽値
  - `doneAt` — ISO8601 文字列（未完了時は空）
  - `createdAt` — ISO8601 文字列

その他のキー: `mytasks.theme`（`light` | `dark`）

## 6. 外部連携
- 認証: なし（単一ユーザー・ローカル完結）
- DB: ブラウザ localStorage のみ。サーバー送信なし
- API: なし（外部リクエストゼロ。オフラインで動作）
- ホスティング: GitHub Pages（https://tsubasagit.github.io/mytasks/ ／ main ブランチのルートを配信）。ローカルでは `index.html` を直接開くか、ローカル HTTP サーバー

## 7. ビジネスルール
- **「今日」ビューには期限を切ったタスクだけが出る。** 期限なしタスクは「すべて」にのみ表示され、今日の一覧を汚さない。
- **並び順**は 期限昇順 → 優先度（最優先>高>通常>低）→ 作成順。期限なしは最後。
- **繰り返しタスク**は完了した瞬間に次回分を新規タスクとして生成する。基準日は元の期限（過去なら今日）。
- **追加したタスクが現在のビューで見えない場合**は自動的に「すべて」へ切り替える（追加したのに消えたように見える事故を防ぐ）。
- **JSON 読み込みは追記マージ**。既存 `id` と重複するタスクはスキップし、既存データを上書きしない。
- **「古い完了を整理」は完了から30日超のみ削除。** 未完了タスクは絶対に消えない。

## 8. 非機能要件
- 想定ユーザー数: 1
- パフォーマンス目標: 数千件までは体感遅延なし（全件を都度描画する単純実装）
- セキュリティ: データは端末外に出ない。バックアップは JSON 書き出しで各自管理
- 依存: なし（外部CDN・ライブラリ・ビルド不要。ファイル1枚）

## 9. 既知の課題・制限
- localStorage のため、**ブラウザのデータ削除・シークレットウィンドウ・別端末ではデータが共有されない**。定期的な JSON 書き出しを推奨。
- **オリジンごとにデータが分かれる。** GitHub Pages 版・ローカル版・PC・スマホはそれぞれ独立したリストになる。移行は JSON 書き出し／読み込みで行う。
- `file://` で開いた場合、ブラウザによっては localStorage が使えず保存されないことがある（保存失敗時はトーストで通知）。確実に使うならローカルHTTPサーバー経由で開く。
- タスク件数が数千件を超えると、1操作ごとの全件再描画が重くなる可能性がある。
- 期限は日付のみ。時刻・リマインド通知は未対応。

## 10. 更新履歴
| 日付 | 内容 |
|---|---|
| 2026-08-29 | 初版作成。MVP実装・動作確認完了 |
| 2026-08-29 | GitHub 公開（tsubasagit/mytasks）・GitHub Pages で配信開始 |
| 2026-08-29 | UI全面リデザイン（デザイントークン整理・優先度カラーバー・タグの面塗り化・ダーク/ライト両テーマ調整） |
| 2026-08-29 | 「オーロラ×ガラス」ショーピース化。View Transitions によるリストモーフ、完了演出（チェック描画・バースト・紙吹雪）、ライブ解釈チップ、完了率リング、円形テーマリビール等を追加 |
| 2026-08-29 | 画面の簡素化。記法ヒント行と検索欄（＋全文検索・`/` ショートカット）を削除 |
| 2026-08-29 | ガラス質感を強化（1pxグラデーションのリム光・鏡面インセット・blur 22-24px/彩度2倍・オーロラ4層に増量）。ホバーを大幅強調（浮き上がり・拡大・グロー・スイープ光沢） |
